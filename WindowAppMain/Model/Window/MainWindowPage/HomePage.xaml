<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WindowAppMain.Model.Window.MainWindowPage"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="WindowAppMain.Model.Window.MainWindowPage.HomePage"
      mc:Ignorable="d" 
      d:DesignHeight="400"
      Title="HomePage"
      Margin="15 0 0 0">
    <Page.Resources>
        <Style x:Key="DayTextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextTrimming" Value="None"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBlockColorWhite}"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <!--<Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="0" Color="white" BlurRadius="25"/>
                </Setter.Value>
            </Setter>-->
        </Style>
        <Style x:Key="TriggersNoteIcon" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{DynamicResource SecondaryAccentBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryAccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryAccentForegroundBrush}"/>
            <Setter Property="materialDesign:ButtonProgressAssist.IndicatorForeground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Style.BasedOn>
                <Style TargetType="{x:Type ButtonBase}">
                    <Setter Property="FocusVisualStyle">
                        <Setter.Value>
                            <Style>
                                <Setter Property="Control.Template">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <Rectangle Margin="2" SnapsToDevicePixels="True" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidForegroundBrush}"/>
                    <Setter Property="materialDesign:RippleAssist.Feedback" Value="White"/>
                    <Setter Property="materialDesign:ButtonProgressAssist.IndicatorForeground" Value="{DynamicResource SecondaryAccentBrush}"/>
                    <Setter Property="materialDesign:ButtonProgressAssist.IndicatorBackground" Value="{DynamicResource MaterialDesignDivider}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
                    <Setter Property="Padding" Value="1"/>
                    <Setter Property="Width" Value="40"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="materialDesign:ButtonProgressAssist.Opacity" Value="1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ButtonBase}">
                                <Grid>
                                    <AdornerDecorator CacheMode="{Binding (materialDesign:ShadowAssist.CacheMode), RelativeSource={RelativeSource Self}}">
                                        <Ellipse x:Name="border" Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}">
                                            <Ellipse.Effect>
                                                <Binding Path="(materialDesign:ShadowAssist.ShadowDepth)" RelativeSource="{RelativeSource TemplatedParent}">
                                                    <Binding.Converter>
                                                        <materialDesign:ShadowConverter/>
                                                    </Binding.Converter>
                                                </Binding>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                    </AdornerDecorator>
                                    <ProgressBar x:Name="ProgressBar" Background="{Binding (materialDesign:ButtonProgressAssist.IndicatorBackground), RelativeSource={RelativeSource TemplatedParent}}" Foreground="{Binding (materialDesign:ButtonProgressAssist.IndicatorForeground), RelativeSource={RelativeSource TemplatedParent}}" HorizontalAlignment="Stretch" IsIndeterminate="{Binding (materialDesign:ButtonProgressAssist.IsIndeterminate), RelativeSource={RelativeSource TemplatedParent}}" Maximum="{Binding (materialDesign:ButtonProgressAssist.Maximum), RelativeSource={RelativeSource TemplatedParent}}" Margin="-8" Minimum="{Binding (materialDesign:ButtonProgressAssist.Minimum), RelativeSource={RelativeSource TemplatedParent}}" Opacity="{Binding (materialDesign:ButtonProgressAssist.Opacity), RelativeSource={RelativeSource TemplatedParent}}" RenderTransformOrigin="0.5,0.5" Style="{DynamicResource MaterialDesignCircularProgressBar}" Value="{Binding (materialDesign:ButtonProgressAssist.Value), RelativeSource={RelativeSource TemplatedParent}}" VerticalAlignment="Stretch">
                                        <ProgressBar.Height>
                                            <TemplateBinding Property="Height">
                                                <TemplateBinding.ConverterParameter>
                                                    <System:Int32>8</System:Int32>
                                                </TemplateBinding.ConverterParameter>
                                                <TemplateBinding.Converter>
                                                    <materialDesign:MathConverter Operation="Add"/>
                                                </TemplateBinding.Converter>
                                            </TemplateBinding>
                                        </ProgressBar.Height>
                                        <ProgressBar.RenderTransform>
                                            <TransformGroup>
                                                <ScaleTransform ScaleY="0" ScaleX="0"/>
                                            </TransformGroup>
                                        </ProgressBar.RenderTransform>
                                        <ProgressBar.Visibility>
                                            <Binding Path="(materialDesign:ButtonProgressAssist.IsIndicatorVisible)" RelativeSource="{RelativeSource TemplatedParent}">
                                                <Binding.Converter>
                                                    <BooleanToVisibilityConverter/>
                                                </Binding.Converter>
                                            </Binding>
                                        </ProgressBar.Visibility>
                                        <ProgressBar.Width>
                                            <TemplateBinding Property="Width">
                                                <TemplateBinding.ConverterParameter>
                                                    <System:Int32>8</System:Int32>
                                                </TemplateBinding.ConverterParameter>
                                                <TemplateBinding.Converter>
                                                    <materialDesign:MathConverter Operation="Add"/>
                                                </TemplateBinding.Converter>
                                            </TemplateBinding>
                                        </ProgressBar.Width>
                                    </ProgressBar>
                                    <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                                    <materialDesign:Ripple ClipToBounds="True" Clip="{Binding RenderedGeometry, ElementName=GeometryEllipse}" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Focusable="False" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    <Ellipse x:Name="GeometryEllipse" Focusable="False" Fill="Transparent" IsHitTestVisible="False"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="Button.IsDefaulted" Value="True">
                                        <Setter Property="Stroke" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.23"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="materialDesign:ShadowAssist.Darken" TargetName="border" Value="True"/>
                                    </Trigger>
                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter Property="materialDesign:ShadowAssist.Darken" TargetName="border" Value="True"/>
                                    </Trigger>
                                    <Trigger Property="materialDesign:ButtonProgressAssist.IsIndicatorVisible" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Duration="0:0:0.2" From="0.9" To="1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" Storyboard.TargetName="ProgressBar"/>
                                                    <DoubleAnimation Duration="0:0:0.2" From="0.9" To="1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" Storyboard.TargetName="ProgressBar"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Duration="0:0:0.2" From="1" To="0.9" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" Storyboard.TargetName="ProgressBar"/>
                                                    <DoubleAnimation Duration="0:0:0.2" From="1" To="0.9" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" Storyboard.TargetName="ProgressBar"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="UIElement.MouseEnter">
                            <BeginStoryboard>
                                <Storyboard  Storyboard.TargetProperty="Foreground.Color" >
                                    <ColorAnimation To="Chartreuse" Duration="0:0:0.1" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="UIElement.MouseLeave">
                            <BeginStoryboard>
                                <Storyboard  Storyboard.TargetProperty="Foreground.Color" >
                                    <ColorAnimation To="Black" Duration="0:0:0.1" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </Style.BasedOn>
        </Style>
        <Style x:Key="LineBorderStyle" TargetType="{x:Type Line}">
            <Setter Property="StrokeThickness" Value="3"/>
            <Setter Property="Stroke" Value="{DynamicResource TextBlockColorChartreuse}"/>
        </Style>
        <Style x:Key="SheldueTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextTrimming" Value="None"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <!--ScrollBar-->

        <Style x:Key="ScrollThumbs" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid x:Name="Grid">
                            <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto" 
                                           Height="Auto" Fill="Transparent"/>
                            <Border x:Name="Rectangle1" CornerRadius="5 5 5 5" HorizontalAlignment="Stretch" 
                                        VerticalAlignment="Stretch" Width="Auto" Height="Auto" 
                                        Background="{TemplateBinding Background}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Horizontal">
                                <Setter TargetName="Rectangle1" Property="Width" Value="Auto"/>
                                <Setter TargetName="Rectangle1" Property="Height" Value="7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Foreground" Value="Chartreuse"/>
            <Setter Property="Background" Value="DarkGray"/>
            <Setter Property="Width" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="5" HorizontalAlignment="Right" Background="{x:Null}">
                            <Track x:Name="PART_Track" Grid.Row="0" IsDirectionReversed="true" Focusable="False">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}" 
                                               Style="{DynamicResource ScrollThumbs}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton x:Name="PageUp" Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton x:Name="PageDown" Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger SourceName="Thumb" Property="IsMouseOver" Value="true">
                                <Setter Value="{DynamicResource ButtonSelectBrush}" 
                                            TargetName="Thumb" Property="Background"/>
                            </Trigger>
                            <Trigger SourceName="Thumb" Property="IsDragging" Value="true">
                                <Setter Value="{DynamicResource DarkBrush}" TargetName="Thumb" Property="Background"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="GridRoot" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="PART_Track" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Width" Value="Auto"/>
                                <Setter Property="Height" Value="5"/>
                                <Setter TargetName="Thumb" Property="Tag" Value="Horizontal"/>
                                <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                                <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--ScrollBar-->
        <Storyboard x:Key="ShowPage">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="MainSheldueGrid">
                <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Page.Resources>
    <Grid x:Name="MainSheldueGrid">
        <ScrollViewer x:Name="ScrollViewerSheldue" VerticalScrollBarVisibility="Auto" Opacity="0" VirtualizingPanel.ScrollUnit="Pixel">
            <ItemsControl x:Name="ListViewSheldueDay" VerticalAlignment="Center" HorizontalAlignment="Center" VirtualizingPanel.IsContainerVirtualizable="True" ItemsSource="{Binding SheldueAllDays}"  VirtualizingPanel.VirtualizationMode="Recycling">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Block !-->
                        <Grid Style="{DynamicResource BlockGridStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Rectangle Grid.RowSpan="2" Style="{DynamicResource BlockRectangleStyle}"/>
                            <TextBlock Grid.Row="0" Text="{Binding DayName}" Style="{DynamicResource DayTextBlockStyle}" />
                            <Grid Grid.Row="1" Margin="5 0 5 5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="35"/>
                                    <ColumnDefinition Width="35"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="45"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <!-- Background Rectangle -->
                                <Rectangle Grid.ColumnSpan="5" Grid.RowSpan="6" Style="{DynamicResource BlockSheldueRectangleStyle}"/>
                                <!-- Background Rectangle -->

                                <!-- Line Table -->

                                <!-- Line Vertical -->
                                <Line Grid.ColumnSpan="5" X1="0" Y1="35" X2="370" Y2="35" Style="{DynamicResource LineBorderStyle}"/>
                                <Line Grid.ColumnSpan="5" Grid.RowSpan="2" X1="0" Y1="87" X2="370" Y2="87" Style="{DynamicResource LineBorderStyle}"/>
                                <Line Grid.ColumnSpan="5" Grid.RowSpan="3" X1="0" Y1="140" X2="370" Y2="140" Style="{DynamicResource LineBorderStyle}"/>
                                <Line Grid.ColumnSpan="5" Grid.RowSpan="4" X1="0" Y1="193" X2="370" Y2="193" Style="{DynamicResource LineBorderStyle}"/>
                                <Line Grid.ColumnSpan="5" Grid.RowSpan="5" X1="0" Y1="246" X2="370" Y2="246" Style="{DynamicResource LineBorderStyle}"/>
                                <!-- Line Vertical -->

                                <!-- Line Horizintal !-->
                                <Line  Grid.RowSpan="6" X1="35" Y1="0" X2="35" Y2="300" Style="{DynamicResource LineBorderStyle}"/>
                                <Line  Grid.RowSpan="6" Grid.Column="1" X1="35" Y1="0" X2="35" Y2="300" Style="{DynamicResource LineBorderStyle}"/>
                                <Line  Grid.RowSpan="6" Grid.Column="2" X1="165" Y1="0" X2="165" Y2="300" Style="{DynamicResource LineBorderStyle}"/>
                                <Line  Grid.RowSpan="6" Grid.Column="3" X1="90" Y1="0" X2="90" Y2="300" Style="{DynamicResource LineBorderStyle}"/>
                                <!-- Line Horizintal !-->

                                <!-- Line Table -->

                                <!-- Button materialdesign noteadd -->
                                <Button Grid.Row="1" Style="{DynamicResource TriggersNoteIcon}" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="Black" Width="Auto" >
                                    <materialDesign:PackIcon Kind="NoteAddOutline" Height="20" Width="20"/>
                                </Button>
                                <Button Grid.Row="2" Style="{DynamicResource TriggersNoteIcon}" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="Black" Width="Auto" >
                                    <materialDesign:PackIcon Kind="NoteAddOutline" Height="20" Width="20"/>
                                </Button>
                                <Button Grid.Row="3" Style="{DynamicResource TriggersNoteIcon}" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="Black" Width="Auto" >
                                    <materialDesign:PackIcon Kind="NoteAddOutline" Height="20" Width="20"/>
                                </Button>
                                <Button  Grid.Row="4" Style="{DynamicResource TriggersNoteIcon}" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="Black" Width="Auto" >
                                    <materialDesign:PackIcon Kind="NoteAddOutline" Height="20" Width="20"/>
                                </Button>
                                <Button Grid.Row="5" Style="{DynamicResource TriggersNoteIcon}" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="Black" Width="Auto" >
                                    <materialDesign:PackIcon Kind="NoteAddOutline" Height="20" Width="20"/>
                                </Button>
                                <!-- Button materialdesign noteadd -->

                                <!-- Upper Textblock !-->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Метка" Style="{DynamicResource SheldueTextStyle}" FontSize="11"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="   № пары" Style="{DynamicResource SheldueTextStyle}" />
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="Предмет" Style="{DynamicResource SheldueTextStyle}" />
                                <TextBlock Grid.Row="0" Grid.Column="3" Text="Препод." Style="{DynamicResource SheldueTextStyle}" />
                                <TextBlock Grid.Row="0" Grid.Column="4" Text="Каб." Style="{DynamicResource SheldueTextStyle}" />
                                <!-- Upper Textblock !-->
                                <ItemsControl x:Name="BlockNametest" Grid.RowSpan="6" Grid.ColumnSpan="5" ItemsSource="{Binding Day}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Grid.RowSpan="6" Grid.ColumnSpan="5">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="35"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <ItemsControl Grid.Row="1" ItemsSource="{Binding Para1}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Grid.Row="1" Height="52">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="90"/>
                                                                    <ColumnDefinition Width="45"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Para}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding Work}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding Teacher}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="1" Grid.Column="4" Text="{Binding Auditorya}" Style="{DynamicResource SheldueTextStyle}" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <ItemsControl Grid.Row="2" ItemsSource="{Binding Para2}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Grid.Row="2" Height="52">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="90"/>
                                                                    <ColumnDefinition Width="45"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Para}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Work}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="2" Grid.Column="3" Text="{Binding Teacher}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="2" Grid.Column="4" Text="{Binding Auditorya}" Style="{DynamicResource SheldueTextStyle}" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <ItemsControl Grid.Row="3" ItemsSource="{Binding Para3}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Grid.Row="3" Height="52">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="90"/>
                                                                    <ColumnDefinition Width="45"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Para}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding Work}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="3" Grid.Column="3" Text="{Binding Teacher}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="3" Grid.Column="4" Text="{Binding Auditorya}" Style="{DynamicResource SheldueTextStyle}" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <ItemsControl Grid.Row="4" ItemsSource="{Binding Para4}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Grid.Row="4" Height="52">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="90"/>
                                                                    <ColumnDefinition Width="45"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Para}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding Work}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="4" Grid.Column="3" Text="{Binding Teacher}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="4" Grid.Column="4" Text="{Binding Auditorya}" Style="{DynamicResource SheldueTextStyle}" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <ItemsControl Grid.Row="5" ItemsSource="{Binding Para5}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Grid.Row="5" Height="52">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="90"/>
                                                                    <ColumnDefinition Width="45"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Para}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="5" Grid.Column="2" Text="{Binding Work}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="5" Grid.Column="3" Text="{Binding Teacher}" Style="{DynamicResource SheldueTextStyle}" />
                                                                <TextBlock Grid.Row="5" Grid.Column="4" Text="{Binding Auditorya}" Style="{DynamicResource SheldueTextStyle}" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Grid>
                        <!-- Block !-->
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
